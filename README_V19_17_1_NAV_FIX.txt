ETB2B Awards V19.17.1 - Public Hero & Navigation Fix
Base: ETB2B Awards V19.17 Glimpse 3D + Scroll Top + About Fix only.

Changes:
- Removed the large visual gap between public navigation and hero content.
- Reworked sticky navigation to avoid scroll lag/layout jump.
- Full two-row premium header stays at the top of the page in normal flow.
- After the full header scrolls away, a separate compact navigation row becomes fixed at the viewport top.
- Compact nav contains only navigation links (no Express Interest/Nominate CTA).
- Improved active state, hover state, spacing and mobile/tablet horizontal navigation.
- Removed expensive sticky backdrop blur from the fixed compact bar for smoother scrolling.
